# 项目状态

更新日期：2026-08-08
基准分支：`main`
基准提交：`f72a522`（详细战斗日志与视觉战斗反馈）

## 阶段结论

项目已完成当前文档覆盖的主要玩法和界面迭代，处于“核心玩法可运行、功能迭代基本完成、工程化收尾待补”的阶段。

## 已完成功能

- 英雄系统扩展：特质与重随、五选一专精、称号解锁与切换、战后英雄统计；专精改为“名称预选—详情预览—确认学习”，预选不写入存档。
- 加载体验：像素风加载覆盖层、字体与贴图加载进度、完成后自动隐藏。
- 英雄与魔物详情：状态/详情拆分、特质品质颜色、称号分页；招募特质、英雄被动和魔物长说明统一通过详情弹层查看。
- 存档：导出与导入。
- 锻造：品质、符文和淬炼扩展。
- 界面修复：新增按 PIXI 实际字体测量的受限文本、长说明分页弹层和可指定绘制层的分页器；固定尺寸卡片不再只限制计算高度而继续越界绘制。
- 锻造交互：胚体、铭文、淬火均提供模态层内可点击分页按钮，保留当前选择，并支持左右键换页。
- 战斗反馈：圣骑士资源统一面向右；受击反应与技能短台词显示在角色头顶并随暂停冻结；行动顺序重构为当前房间交战单位共用的右向左头像行动条。

## 当前验证状态

- 11 个当前核心 JavaScript 文件通过 `node --check` 语法检查，`git diff --check` 通过。
- 入口 HTML、核心脚本、PIXI、字体和主要 UI 资源可由本地静态服务正常读取；圣骑士图片保持 256×256、32 位透明通道。
- 浏览器端已验证：专精预选/确认、三类锻造分页实际点击、长说明分页、圣骑士朝向、行动条推进、技能/受击气泡及暂停冻结；浏览器控制台无错误。
- 已在 480×270 逻辑分辨率和 1280×720 显示尺寸检查受限文本边界，当前记录为零越界。
- 仓库目前没有正式自动化测试框架或构建脚本；本轮验证使用一次性浏览器断言，后续应固化为可重复运行的测试。

## 仓库约定

- 当前入口只使用 `index.html` 和 `game.js`；历史编号副本已删除，版本回溯统一使用 Git。
- 正式图片与音频资源统一放在 `assets/`；根目录测试截图和 Playwright 临时截图不纳入版本控制。
- 实现计划的复选框代表当前代码完成状态，不再仅代表最初编写计划时的状态。

## 后续优先级

1. 将本轮一次性浏览器断言固化为仓库内冒烟测试，继续覆盖结算和存档导入导出。
2. 增加统一的本地启动与验证命令。
3. 完成发布前浏览器兼容性、存档兼容性和完整玩法回归。
